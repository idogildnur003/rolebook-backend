$kind: http-request
url: "{{baseUrl}}/campaigns/{{campaignId}}/sessions/{{sessionId}}/confirmed-slot"
method: DELETE
headers: {}
body:
  type: none
scripts:
  - type: afterResponse
    code: |-
      pm.test("Status 204", () => pm.response.to.have.status(204));
    language: text/javascript
order: 4011
