$kind: http-request
url: "{{baseUrl}}/campaigns/{{campaignId}}/players"
method: GET
scripts:
  - type: afterResponse
    code: pm.test("Status 200", () => pm.response.to.have.status(200));
    language: text/javascript
order: 2000
