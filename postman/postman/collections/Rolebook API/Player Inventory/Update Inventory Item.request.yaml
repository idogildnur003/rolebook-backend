$kind: http-request
url: "{{baseUrl}}/players/{{playerId}}/inventory/{{playerEquipmentId}}"
method: PATCH
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "quantity": 2
    }
scripts:
  - type: afterResponse
    code: pm.test("Status 204", () => pm.response.to.have.status(204));
    language: text/javascript
order: 3000
