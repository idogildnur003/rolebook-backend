$kind: http-request
url: "{{baseUrl}}/campaigns/{{campaignId}}/custom-spells/{{customSpellId}}"
method: DELETE
scripts:
  - type: afterResponse
    code: pm.test("Status 204", () => pm.response.to.have.status(204));
    language: text/javascript
order: 4000
